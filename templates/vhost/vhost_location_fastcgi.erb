  location <%= @location %> {
    root  <%= @www_root %>;
    include <%= @fastcgi_params %>;
    fastcgi_pass <%= @fastcgi %>;
<% if @fastcgi_script %>
    fastcgi_param SCRIPT_FILENAME <%= @fastcgi_script %>;
<% end -%>
  }

